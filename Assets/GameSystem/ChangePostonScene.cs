using UnityEngine;

public class ChangePostonScene : MonoBehaviour
{
    



    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //GetComponent<Volume>.profile.TryGet<ChromaticAberration>(out var chromaticAberration);


    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
